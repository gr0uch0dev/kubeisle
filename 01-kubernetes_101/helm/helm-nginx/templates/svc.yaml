apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.nginxService.name }}
  namespace: {{ .Values.metadata.namespace }}
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 80
  selector:
    app: nginx

